import 'package:flutter/material.dart';

class EventFormModal extends StatelessWidget {
  const EventFormModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
